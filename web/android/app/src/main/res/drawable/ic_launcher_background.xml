<?xml version="1.0" encoding="utf-8"?>
<!--
   Adaptive icon background.

   Was the Android Studio template vector: a teal #26A69A tile with a light
   grid drawn over it, which is why the app's icon on the device home screen
   and in the app switcher had nothing to do with Rheoson. A flat brand plate
   lets the logo (mipmap-*/ic_launcher_foreground.png) carry the identity and
   matches the app's own background colour.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportHeight="108"
    android:viewportWidth="108">
    <path
        android:fillColor="#0A0A0A"
        android:pathData="M0,0h108v108h-108z" />
</vector>
