# For the APK build use web/.env.production.
# For Render web service: set VITE_API_URL in the Render dashboard
# under Environment → Environment Variables.

# Backend API origin — no trailing slash
# Dev:  leave blank (Vite proxy handles /api → localhost:8000)
# Prod: https://shulker-api-vnny.onrender.com
VITE_API_URL=